{
  "name": "multibase-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for Multibase",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
